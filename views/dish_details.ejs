<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <style>
        body {
            text-align: center;
        }
        img {
            max-width: 200px;
            aspect-ratio: 1/1 ;
        }
    </style>
</head>
<body>
    <h1><%= dish_details[0].title %></h1>
    <img src="<%= dish_details[0].image_url %>" alt="">
    <h2><%= dish_details[0].description %></h2>


    <form action="/dishes/<%= dish_details[0].id %>" method="post">
        <input type="hidden" name="_method" value="delete" />
        <input type="hidden" name="dish_id" value="<%=dish_details[0].id%>"/>
        <button>delete</button>
    </form>
    <a href="/dishes/<%= dish_details[0].id %>/edit">edit</a>
</body>
</html>